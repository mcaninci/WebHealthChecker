

import React from 'react';
import * as eva from '@eva-design/eva';
import { Icon,ApplicationProvider, IconRegistry,Button, Layout, Text } from '@ui-kitten/components';
import { EvaIconsPack } from '@ui-kitten/eva-icons';

export const CameraIcon = (props) => (
  <Icon name='camera' {...props} />
);
export const FacebookIcon = (props) => (
  <Icon name='facebook' {...props} />
);
export const EmailIcon = (props) => (
  <Icon name='email' {...props} />
);
export const GoogleIcon = (props) => (
  <Icon name='google' {...props} />
);

export const TwitterIcon = (props) => (
  <Icon name='twitter' {...props} />
);


export const PersonIcon = (props) => (
  <Icon name='person' {...props} />
);

export const PlusIcon = (props) => (
  <Icon name='plus' {...props} />
);

